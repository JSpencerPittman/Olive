# Unions & Structures
UNION__DEF := KEYWORD__UNION ( IDENTIFIER ) ? { ( VARIABLE__DEC ) * } ( IDENTIFIER ) ? ;
STRUCT__DEF := KEYWORD__STRUCT ( IDENTIFIER ) ? { ( ( VARIABLE__DEC ) | ) * } ( IDENTIFIER ) ? ;
SPECIAL-RULE := CAPTURE-NESTED-DEFS

# Enums
ENUM__START := KEYWORD__ENUM ( IDENTIFIER ) ? {
SPECIAL-RULE := CONSOLIDATE-ENUM-CONTENTS
ENUM__DEF := ENUM__START ( IDENTIFIER ( ENUM_CONTENTS ) ? , ) * IDENTIFIER ( ENUM_CONTENTS ) ? } ;

# Typedef
TYPEDEF := KEYWORD__TYPEDEF ( STRUCT__DEF UNION__DEF VARIABLE__DEC ENUM__DEF ( ( KEYWORD__STRUCT KEYWORD__UNION ) | VARIABLE__DEC ) ) |
