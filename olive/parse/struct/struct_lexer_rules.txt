# Unions & Structures
UNION__DEF := KEYWORD__UNION ( IDENTIFIER ) ? { ( VARIABLE__DEC ) * } ( IDENTIFIER ) ? ;
STRUCT__DEF := KEYWORD__STRUCT ( IDENTIFIER ) ? { ( ( VARIABLE__DEC ) | ) * } ( IDENTIFIER ) ? ;
SPECIAL-RULE := CAPTURE-NESTED-DEFS

# Enums
ASSIGNMENT(10) := ( = ( , } ) | ) .
ENUM__DEF := KEYWORD__ENUM ( IDENTIFIER ) ? { ( IDENTIFIER ( ASSIGNMENT ) ? , ) * IDENTIFIER ( ASSIGNMENT ) ? } ( IDENTIFIER ) ? ;

# Typedef
TYPEDEF__DEF := KEYWORD__TYPEDEF ( STRUCT__DEF UNION__DEF VARIABLE__DEC ENUM__DEF ) |
TYPEDEF__REFERENCE := KEYWORD__TYPEDEF ( KEYWORD__STRUCT KEYWORD__UNION ) | VARIABLE__DEC
